// https://leetcode.com/problems/path-sum-iii/description/

/*
 * Will divide problem in sub problem
 * first starting with the main root will count ways from that
 * then we will consider root.left as the only root and get the answer
 * same, for root.right and at last with the TC: O(N^2) we will get the answer
 */
package Trees;

public class KSumPaths {
    // ! Not solved as of now.
}
